# BASE-001 本地基线追溯检查

> 验收 ID：`BASE-001`
>
> 证据日期：2026-07-18
>
> 当前判定：`进行中`。
>
> 证据边界：非结构化状态声明与 API 方法、路径、关键请求字段的可执行验证器已实现；但本页不把正在编辑的工作区自证为最终候选。只有根 Agent 在最终干净 HEAD 上以 exact commit SHA 重跑验证器并把外部 JSON/原始日志归档后，才形成候选绑定证据。该本地证据仍不替代受保护 CI、真实基础设施、外部服务、DGX ARM64、告警或灾备证据，因此 `BASE-001` 继续保持“进行中”。

## 权威链

| 职责 | 权威源 | 下游引用 |
|---|---|---|
| 项目红线与必读优先级 | [AGENTS.md](../../AGENTS.md) | [权威文档索引](../../需求文档/README.md) |
| 产品范围与完成定义 | [01 PRD](../../需求文档/01_PRD_产品需求文档.md) | README、IA、验收矩阵 |
| 文件状态与 HTTP 契约 | [05 状态机/API](../../需求文档/05_DATABASE_API_SPEC_数据库与API规范.md) | PRD、架构、后端、前端、AI/RAGFlow、IA、视觉设计 |
| 页面与角色工作台 | [角色 IA](./IA_ROLE_WORKBENCH.md) | README、前端规范 |
| 运行时业务配置 | [配置契约](./CONFIG_CONTRACT.md) | 部署规范、管理 API 与设置页 |
| 阶段与逐项验收 | [任务拆解](../../需求文档/08_TASK_BREAKDOWN_开发任务拆解.md)及[验收矩阵](./ACCEPTANCE_MATRIX.md) | [README](../../README.md) 当前阶段声明 |

05 是唯一的文件状态和 HTTP 目标契约。其他文档可以描述各自职责内的交互或实现方式，但必须显式回指 05，不能建立第二份允许转换或 API 真源。

## 开发期可重复检查

安装后端开发依赖后，在项目根执行：

```powershell
python -m pytest ops/tests/test_baseline_document_contract.py -q
```

2026-07-18 工作区观察结果：`13 passed`。该数字只是可重放的开发检查记录。测试固定验证：

1. AGENTS 必读路径和章节引用存在，所有权威 Markdown 相对链接可解析且不逃出仓库；
2. PRD、架构、后端、前端、AI/RAGFlow、IA、视觉设计与任务拆解都显式回指 05；
3. 结构化、自然语言、ASCII/Unicode 图示及分支续行中的技术状态声明都被扫描或显式分类；
4. 任何新增但未分类的非结构化状态声明 fail closed，非法结构化边也会失败；
5. 05 与运行时业务 OpenAPI 的方法/路径双向逐项相等；非业务路由只能进入有理由的精确排除集；关键请求体、查询字段、必填性、枚举、长度和分页边界一致；
6. 05 的持久化状态目录与视觉状态映射逐项相等，且不引入第二个 `draft` 状态；
7. README、前端规范和 IA 的产品路由集合一致；
8. 配置契约恰好是 26 个 active key 与 15 个 deleted key，集合互斥；
9. README、PRD、权威索引、任务拆解和配置契约均保持“阶段 9 未完成”的发布边界；
10. 验收矩阵 ID 唯一且状态值合法；
11. 候选证据验证器要求 exact 40 位 SHA、tracked 与非忽略 untracked 都为空的干净工作区、空输出目录，并明确记录外部发布状态未评估；
12. `BASE-001` 指向本检查记录、保持“进行中”，不以开发期测试自证最终候选完成；
13. 验证器源码、测试和全部权威文档均进入输入摘要，验证期间任何变化都会使证据生成失败。

### 非结构化状态声明

验证器逐行扫描全部权威文档。包含至少两个技术状态的自然语言/分组，或在反引号外使用箭头的图示，都必须绑定一条精确的人工处置：允许边或仅状态集合。反引号内的结构化边直接解析；明确否定的非法边必须与允许边集合不相交。当前清单覆盖：

- 19 个持久化状态、48 条 05 允许边；
- 33 条候选声明，其中 14 条自动解析、11 条图示/自然语言允许边、8 条仅状态集合；
- 48 条允许边均至少被扫描结果引用，并识别 3 条明确禁止边。

处置以“文档路径 + 完整语句”绑定，证据只保存语句 SHA256 与行号。修改语句、增加新关系或删掉已审计声明都会要求重新分类，不能通过模糊关键词白名单绕过。

### API 方法、路径与字段语义

验证器从 05 的显式表格和内联方法/路径派生目标端点，再与 FastAPI OpenAPI 对照；05 仍是唯一权威源，验证器不是第二份 API 规范。当前检查覆盖：

- 105 个业务方法/路径（88 个不同路径），与运行时 OpenAPI 双向逐项相等，禁止 `GET/PUT` 或 `retry|cancel` 这类不可判定简写；
- 13 组关键 JSON/multipart 请求体的完整字段集、必填性与枚举，包括保存视图创建/更新；
- 11 组列表/内容/配置/审计/统计/保存视图查询的完整参数集、枚举、`q` 最大长度及统一分页边界；
- 9 个非业务方法/路径的最小排除项：FastAPI OpenAPI、Swagger UI、OAuth redirect、ReDoc 的 GET/HEAD，以及 Prometheus `GET /metrics`；每项理由都进入 JSON 证据。

本轮同时修正了配置读取、审计查询和通知已读路径漂移，补记上传版本替代、敏感确认及兼容注册字段语义，并纳入保存视图的服务端分页/搜索、每命名空间 100 条并发配额与遗留超额可治理边界。运行时行为与权限仍由各业务验收 ID 和 API/E2E 测试证明，不能由 OpenAPI 存在性代替。

## 最终候选绑定

代码全部整合并提交后，由根 Agent 在最终干净 HEAD 上执行一次，输出目录必须位于提交之外且为空：

```powershell
$sha = (git rev-parse HEAD).Trim()
python scripts/check_baseline_contract.py `
  --expected-git-sha $sha `
  --output-dir "C:\tmp\knowledge-uploader-baseline-$sha"
```

验证器在测试前后分别确认 HEAD、Git tree 与 tracked/非忽略 untracked 均为空的工作区，复核全部输入 SHA256，并生成：

- `baseline-contract-<sha>.json`：exact commit/tree、输入摘要、状态/API 审计摘要、测试退出码及证据边界；
- `baseline-contract-<sha>.log`：脱敏结构化摘要，仅包含命令、退出码、通过数量和精确 nodeid，
  不保存 pytest stdout/stderr；JSON 中保存其 SHA256。

SHA 不匹配、tracked 文件有修改、存在非忽略 untracked 文件、输入在运行中改变、测试失败、输出目录非空或证据已存在时一律不写成功证据。当前提交只交付验证能力；最终整合后的 SHA 尚未绑定，所以矩阵不得提前改成“通过”，也不得把 `external_release_status=not_evaluated` 提升为任何外部发布结论。
