# BASE-001 本地基线追溯检查

> 验收 ID：`BASE-001`
>
> 证据日期：2026-07-18
>
> 当前判定：`进行中`。
>
> 证据边界：当前检查只证明工作区内的权威文档、相对链接、可解析的结构化状态转换表达式、状态目录、产品路由、配置表和阶段声明一致。它尚未证明非结构化描述中的转换覆盖完整，也未校验 API 方法、路径和字段语义，且未绑定最终候选 commit 或 CI 原始日志；因此不能独立支撑 `BASE-001` 标记“通过”，更不替代运行时、真实基础设施、外部服务、ARM64 或发布证据。

## 权威链

| 职责 | 权威源 | 下游引用 |
|---|---|---|
| 项目红线与必读优先级 | [AGENTS.md](../../AGENTS.md) | [权威文档索引](../../需求文档/README.md) |
| 产品范围与完成定义 | [01 PRD](../../需求文档/01_PRD_产品需求文档.md) | README、IA、验收矩阵 |
| 文件状态与 HTTP 契约 | [05 状态机/API](../../需求文档/05_DATABASE_API_SPEC_数据库与API规范.md) | PRD、架构、后端、前端、AI/RAGFlow、IA、视觉设计 |
| 页面与角色工作台 | [角色 IA](./IA_ROLE_WORKBENCH.md) | README、前端规范 |
| 运行时业务配置 | [配置契约](./CONFIG_CONTRACT.md) | 部署规范、管理 API 与设置页 |
| 阶段与逐项验收 | [任务拆解](../../需求文档/08_TASK_BREAKDOWN_开发任务拆解.md)及[验收矩阵](./ACCEPTANCE_MATRIX.md) | [README](../../README.md) 当前阶段声明 |

05 是唯一的文件状态和 HTTP 目标契约。其他文档可以描述各自职责内的交互或实现方式，但必须显式回指 05，不能建立第二份允许转换或 API 真源。

## 可重复检查

在项目根执行：

```powershell
python -m pytest ops/tests/test_baseline_document_contract.py -q
```

2026-07-18 工作区观察结果：`9 passed`。该数字是可重放的开发检查记录，不是候选发布证据。测试固定验证：

1. AGENTS 必读路径和章节引用存在，所有权威 Markdown 相对链接可解析且不逃出仓库；
2. PRD、架构、后端、前端、AI/RAGFlow、IA、视觉设计与任务拆解都显式回指 05；
3. 扫描上述下游权威文档中反引号包裹且符合约定语法的状态转换表达式，并证明其边集合均被 05 的允许转换覆盖；该扫描不声称覆盖自然语言、图示或其他非结构化写法；
4. 05 的持久化状态目录与视觉状态映射逐项相等，且不引入第二个 `draft` 状态；
5. README、前端规范和 IA 的产品路由集合一致；
6. 配置契约恰好是 26 个 active key 与 15 个 deleted key，集合互斥；
7. README、PRD、权威索引、任务拆解和配置契约均保持“阶段 9 未完成”的发布边界；
8. 验收矩阵 ID 唯一且状态值合法；
9. `BASE-001` 指向本检查记录、保持“进行中”，且没有把工作区检查自证为候选发布完成。

该结果可在任意工作区重放；后续修改上述文档而未同步引用、结构化状态转换、状态目录、路由、配置计数或阶段声明时，契约测试必须失败。关闭 `BASE-001` 仍需在最终候选 commit 上保存独立命令日志及摘要，并补充非结构化转换的完整覆盖与 API 方法、路径和字段语义冲突扫描。
